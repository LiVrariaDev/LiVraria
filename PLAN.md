# ドキュメント整備計画

## 目的

LiVraria のドキュメントを、初めて開発に参加する人が迷わず環境構築・開発・機能理解をできる状態にする。

現状の文書には AI 生成である旨が記されており、実装・運用手順との一致が未確認である。したがって、文章を増やすことよりも、**実装を根拠に正確性を確認し、情報の置き場と責任範囲を整理すること**を優先する。

Docker は、動作確認が済むまで開発・デプロイの推奨手段から外す。Docker 関連ファイルはこの計画では削除せず、将来再構築するための検討対象として残す。

## 完了条件

- ルートの `README.md` から、ローカル開発を開始するための最新手順へ到達できる。
- 各ドキュメントに対象読者と用途があり、同じ手順・設定値を複数箇所で管理しない。
- 実装で確認できない設計・運用情報は、事実として断定せず、未確認または将来案として区別されている。
- Docker は未検証であることが明確で、通常のセットアップやデプロイ手順として案内されていない。
- リンク切れ、存在しないファイル名、現在のスクリプトと異なるコマンドがないことを確認している。

## 現状の把握

2026-07-21 時点で、主な文書は以下に分散している。

| 場所 | 現在の役割・課題 |
| --- | --- |
| `README.md` | 概要、環境構築、HTTPS、Docker、デプロイ、構成、技術スタックが混在している。`docs/technical_specs.md` へのリンク先は存在しない。 |
| `docs/technical_specs_frontend.md` / `docs/technical_specs_backend.md` | 詳細な設計記述。実装との照合が未完了で、設計案と現行仕様が混在している可能性がある。 |
| `docs/DEPLOYMENT.md` | Docker Compose を「推奨」としている。実際のデプロイ可否を未検証のまま掲載している。 |
| `backend/README.md` / `frontend/README.md` | 各アプリケーション固有の情報。ルート README との重複を確認する必要がある。 |
| `raspi/README.md` / `raspi/nfc/README.md` | Raspberry Pi・NFC 固有のセットアップ情報。ハードウェア依存のため、Web 開発手順から切り離して維持する。 |
| `docker-compose.yml` / 各 `Dockerfile` | Docker 用構成は存在するが、現状で起動・運用できることを確認していない。 |

追記（2026-07-21）:

- 永続データは `backend/api/data/` の JSON ファイルを使用しており、MongoDB は利用していない。
- Raspberry Pi 上での実証は `raspi/2nddisp` ブランチで行われた。現在の `main` での再現性は未確認である。
- 本番公開は Nginx で静的ファイルを配信し、API サーバーへリバースプロキシする構成を前提とする。具体的な稼働手順は未検証である。
- 現行の LLM は Gemini で、実装では `gemini-2.5-flash` を指定している。書籍検索は楽天ブックス API を利用する。
- Docker を再構築する場合の対象は本番環境とする。
- プロジェクトのライセンスは EUPL v1.2 とする。

## 方針

### 情報の正しさ

- コマンド、環境変数、URL、API、ディレクトリ構成は、ソースコード・設定ファイル・実行スクリプトを一次情報として確認する。
- 実装から判断できない内容は、担当者確認が必要な項目として残す。推測で補完しない。
- 設計上の意図、将来案、現行で動作する仕様を見出し・注記で明確に分離する。

### ドキュメントの役割分担

- `README.md`: プロジェクト概要、対応範囲、最短のローカル開発開始手順、詳細文書への入口だけを載せる。
- `docs/`: 横断的な開発・運用・アーキテクチャ情報を置く。
- `backend/`、`frontend/`、`raspi/`: それぞれのコンポーネントに固有の設定・実行・保守手順を置く。
- ある情報の正本を一箇所に決め、他の文書からはリンクする。

### Docker の扱い

- Docker Compose と Dockerfile は、確認が済むまで「未検証・非推奨」として扱う。
- `README.md` のクイックスタートと `docs/DEPLOYMENT.md` から Docker を通常手順・推奨手順として外す。
- Docker 関連ファイルの削除、CI への導入、イメージ公開は、この整備では行わない。
- Docker を再採用する場合は、別タスクでローカル起動、環境変数、フロントエンドから API への到達、永続化、停止・再起動を検証し、その結果に基づいて構成を作り直す。

## 実施計画

### 1. 文書と一次情報の棚卸し

1. すべての Markdown、環境変数テンプレート、起動スクリプト、設定ファイルを一覧化する。
2. 各文書について「対象読者」「正本にする情報」「更新または廃止の要否」を表にする。
3. README 内リンク、参照ファイル、コマンド名を機械的に抽出し、存在確認する。
4. `.env.template`、`package.json`、Python の依存定義、起動スクリプトから、必要な環境変数と実行コマンドの正本を特定する。

成果物: 文書台帳と、修正対象・確認待ち項目の一覧。

### 2. 現行動作の確認

1. Node.js、pnpm、Python の必要バージョンを設定ファイルと実際のセットアップ処理から確認する。
2. `pnpm run setup`、`pnpm run start`、個別起動コマンドについて、依存関係・前提ファイル・起動先ポートを確認する。
3. フロントエンド、バックエンド、Raspberry Pi/NFC の境界と通信先をコードから確認する。
4. 必須・任意・開発環境で不要な環境変数を分類する。秘密値そのものは文書化しない。
5. テスト、ビルド、静的検査の実行方法と、現時点で実行可能な範囲を確認する。

成果物: 「確認済み」「未確認」「実装と不一致」の記録。未確認事項は文書に断定的に記載しない。

### 3. 文書構成の再編

以下の構成を目標とする。既存文書をすぐに大量移動せず、内容の確定後に必要最小限の新規文書・移動を行う。

```text
README.md                         # 概要とローカル開発の入口
docs/
  development.md                  # 開発環境、セットアップ、起動、検証
  configuration.md                # 環境変数の説明と設定方針
  architecture.md                 # 確認済みの構成・通信・責務
  deployment.md                   # 現行のデプロイ状況と未対応事項
  docker.md                       # 未検証・非推奨の理由と再構築条件
backend/README.md                 # バックエンド固有の開発情報
frontend/README.md                # フロントエンド固有の開発情報
raspi/README.md                   # Raspberry Pi 全体の導入・運用情報
raspi/nfc/README.md               # NFC サーバー固有の手順
```

1. `README.md` を短く保ち、概要、対応機能、前提条件、最短手順、各詳細文書へのリンクに絞る。
2. 開発手順は `docs/development.md` を正本とし、README から参照する。
3. 環境変数の説明は `docs/configuration.md` に集約し、`.env.template` と同期させる。
4. 現行実装で確認できた内容だけを `docs/architecture.md` に記載する。
5. 既存の `technical_specs_*.md` は内容を検証して、現行仕様として残す部分、将来案として残す部分、削除する部分を分ける。検証前に単純な統合・削除はしない。

### 4. Docker とデプロイ文書の是正

1. `README.md` の Docker 前提・Docker 起動手順を、未検証である旨と将来の検証先への案内に置き換える。
2. `docs/DEPLOYMENT.md` は、検証済みのデプロイ方法がない限り「本番デプロイ手順」ではなく、現状・前提・未確認項目を示す文書に変更する。
3. `docker-compose.yml`、`backend/Dockerfile`、`frontend/Dockerfile` について、以下を検証項目として記録する。
   - ビルドが成功するか
   - `.env` とサービスアカウント鍵を安全に渡せるか
   - フロントエンドがバックエンド API に到達できるか
   - CORS、公開ポート、リバースプロキシ設定が整合するか
   - データ保存と再起動時の扱いが明確か
4. 検証に合格するまでは Docker をサポート対象に含めない。失敗時は、Docker 関連ファイルを残すか削除するかを別途判断する。

### 5. レビューと継続運用

1. ドキュメント内のすべてのリンク、コマンド、ファイルパスを確認する。
2. 秘密情報、実在の API キー、サービスアカウント鍵への誘導がないことを確認する。
3. 初見の開発者が README だけを起点にローカル起動まで到達できるかをレビューする。
4. 大きな仕様変更時に更新すべき文書を、各文書の冒頭に短く記載する。
5. 文書変更は可能な限り機能変更と同じコミットで更新する運用にする。

## 判断が必要な項目

- 本番環境を現在も運用しているか。運用している場合、その実際の方式と管理者は誰か。
- Raspberry Pi/NFC は `main` に統合して維持する機能か、実験・将来機能か。
- 既存の詳細技術仕様を「設計履歴」として残すか、現行仕様へ更新するか。

## 今回の整備で扱わないこと

- Docker の再実装・本番デプロイ・CI/CD の有効化
- アプリケーション機能、API、データモデルの仕様変更
- 秘密情報や本番インフラ設定のリポジトリへの追加
- 実装確認なしの技術仕様の増補

## 推奨する進め方

まず第 1・2 段階で事実を確定し、その結果をもとに第 3・4 段階の文書改修を行う。一度にすべてを書き換えず、`README.md` と開発手順を最初の小さな変更単位とし、次に環境変数・アーキテクチャ・Raspberry Pi・デプロイの順で進める。
