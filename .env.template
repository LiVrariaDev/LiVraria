# LiVraria `.env.template`
#
# このファイルをコピーして `.env` にリネームし、開発環境やデプロイ環境ごとの値を設定してください。
# `.env` は機密情報を含むため、絶対にリポジトリにコミットしないでください。
# サンプル（PoC向け）の変数一覧を示します。必要に応じて追加してください。

#############################
# Backend / FastAPI
#############################
# Firebase Admin SDK のサービスアカウントJSONファイルの絶対パス、またはJSON文字列
FIREBASE_SERVICE_ACCOUNT_JSON_PATH=./secrets/firebase-service-account.json
# もしくは: FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'

# Gemini API Key (Google Cloud の適切な認証方法に合わせる)
GEMINI_API_KEY=
GEMINI_API_URL=

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=livraria_dev

# FastAPI run host/port
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Environment
PYTHON_ENV=development
LOG_LEVEL=INFO

#############################
# Frontend / Vite (環境変数は Vite ルールに従って `VITE_` で始める)
#############################
# API ベース URL（フロントエンドがアクセスするバックエンドのエンドポイント）
VITE_API_BASE_URL=http://localhost:8000

#############################
# PoC: 運用/保持に関する暫定値（ドキュメントと同期させる）
#############################
# 会話履歴の保持（日数）
CONVERSATIONS_TTL_DAYS=90
# 推薦履歴の保持（推奨: 3年 = 1095日）
RECOMMEND_LOG_TTL_DAYS=1095
# Users.ai_insights の保持（推奨: 3年 = 1095日）
AI_INSIGHTS_TTL_DAYS=1095

#############################
# 注意事項
#############################
# - このテンプレートを `.env` にコピーして実際の値を設定してください:
#     cp .env.template .env
# - `.env` は必ず `.gitignore` に追加してリポジトリに含めないでください。
# - 変更後はバックエンド/フロントエンドを再起動してください。
